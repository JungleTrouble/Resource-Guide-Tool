run = "pip install -r requirements.txt && python -m uvicorn app.main:app --host 0.0.0.0 --port 8080"
language = "python3"

[nix]
channel = "stable-24_05"

[[ports]]
localPort = 8080
externalPort = 80
